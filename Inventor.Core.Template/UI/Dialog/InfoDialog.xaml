<!--  /-:msbuild-conditional:noEmit  -->
<ui:FluentWindow
    x:Class="ExtrabbitCode.Inventor.Core.Template.UI.Dialog.InfoDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="InfoDialog"
    Width="800"
    Height="450"
    ShowInTaskbar="False"
    mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ui:TitleBar
            Title="Info MuM.VoxelAssembler"
            Grid.Row="0"
            Margin="-10,0,0,0"
            CanMaximize="False"
            ShowMaximize="False"
            ShowMinimize="False">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/ExtrabbitCode.Inventor.Core.Template;component/Resources/appIcon.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>
        <Label
            Grid.Row="1"
            VerticalAlignment="Center"
            Content="Program Version:" />
        <TextBox
            Grid.Row="2"
            Margin="0,5,0,15"
            IsReadOnly="True"
            Text="{Binding ProgramVersion}" />

        <Label
            Grid.Row="3"
            VerticalAlignment="Top"
            Content="Version History:" />
        <TextBox
            Grid.Row="4"
            AcceptsReturn="True"
            IsReadOnly="True"
            Text="{Binding VersionHistory}"
            VerticalScrollBarVisibility="Auto" />
    </Grid>
</ui:FluentWindow>
<!--  /+:msbuild-conditional:noEmit  -->